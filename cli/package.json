{
  "name": "@d2c/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "d2c deterministik XD extractor + doğrulayıcı",
  "bin": {
    "d2c": "./dist/d2c.mjs"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "test:only": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@babel/parser": "^8.0.4",
    "pngjs": "^7.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.0"
  },
  "optionalDependencies": {
    "playwright-core": "^1.56.0"
  }
}
